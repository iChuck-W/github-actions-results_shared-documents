<!DOCTYPE html>
<html lang="zh-CN">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🤖 工业品智能专家</title>
    <style>
       /* 应用全局样式 */
        html, body {
            height: 100%;  /* 确保 html 和 body 的高度是 100% */
            overflow-x: hidden; /* 防止水平滚动条出现 */
        }
        /* 应用全局样式 */
        body {
            background-color: #ffffff;  /* 网页整体背景颜色：白色 */
            display: flex; /* 显示方式：块级弹性盒子容器 */
            font-family: Simhei, Helvetica; /* 默认中文字体：黑体，备用西文字体：Helvetica */
            height: 100vh; /* 高度为视口高度的 100% */
            margin: 0;  /* 移除外边距 */
        }
        /* 右侧内容区域样式 */
        .content {
            align-items: center;
            background-color: white;
            border-radius: 10px; 
            display: flex;
            flex-direction: column;
            flex-grow: 1; /* 自动填充剩余空间 */
            padding: 10px 150px; /* 内容区域的内边距，简写形式：上/下px 左/右px */
            overflow-y: auto; /* 垂直滚动：允许 */
            overflow-x: hidden;  /* 水平滚动：禁止 */
        }
        /* 右侧内容区域中标题、段落、无序列表和列表项的通用样式 */
        .content > * {
            background-color: #f5f5f5; /* 文本背景色：浅灰色 */
            border-radius: 10px; 
            max-width: 100%; /* 确保这些元素宽度不超过其父元素 */
            margin-bottom: 20px; 
            padding: 10px;
            text-align: left; /* 对齐方式：左对齐 */
        }
        /* 右侧内容区域中标题的特定样式 */
        .content h2 {
            margin-top: 10px; /* 顶部外边距 */
            width: 100%; /* 设置标题宽度占满内容区域 */
        }
        /* 右侧内容区域中无序列表的特定样式 */
        .content ul {
            list-style-type: disc; /* 无序列表符号：实心圆点 */
            padding-left: 20px; /* 左侧内边距 */
            width: 100%; 
        }
        /* 右侧内容区域中列表项的特定样式 */
        .content li {
            margin-bottom: 10px;
            text-align: left;
        }
        /* 右侧内容区域中图片和iframe的特定样式 */
        .content img, .content iframe {
            background-color: #f5f5f5; /* 图片和 iframe 背景色：浅灰色 */
            border-radius: 10px; 
            display: block; 
            height: auto; /* 保持图片和iframe的宽高比 */
            max-width: 100%; 
            margin: 0 auto; /* 上/下外边距、左右外边距 */
        }
        }
    </style>
</head>
<body>
    <h1>📃 说明</h1>
        <ul>
            <li>文件内容由 AI 生成，方便用户下载后编辑。</li>
        </ul>
    <h1>🤖 工业品智能专家</h1>
        <ul>
             <li><a href="https://www.aibangxuanxing.com" target="_blank">内容由 AI 生成，仅供参考 · 点击进入对话页面</a></li>
        </ul>
</body>
</html>